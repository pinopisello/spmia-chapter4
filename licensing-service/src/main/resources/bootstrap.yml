spring:
  application:
    name: licensingservice
  datasource:
    initialize: false   #runna schema.sql ad ogni boostrap 
  profiles:
    active:
      prod
  cloud:
    config:
      enabled: true
#      discovery:
#        enabled: true
#        serviceId: configserver
